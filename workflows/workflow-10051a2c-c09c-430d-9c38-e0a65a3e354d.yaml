id: 10051a2c-c09c-430d-9c38-e0a65a3e354d
name: generate-ixens
description: Generate one self-contained Ixen minisite per topic under ixen/<slug>/ with hero art, supporting concept images, and a reusable soundtrack.
tags: {}
inputs:
  type: object
  properties:
    ixenApiFormat:
      type: string
      enum:
        - anthropic
        - openai-compat
      default: anthropic
    ixenBaseUrl:
      type: string
      description: Optional base URL override for openai-compatible Ixen generation.
    skillLevel:
      type: string
      enum:
        - novice
        - intermediate
        - senior
        - guru
      default: senior
    outputLength:
      type: string
      enum:
        - short
        - medium
        - long
      default: medium
    ixenModel:
      type: string
      default: claude-opus-4-8
    credits:
      type: string
      default: txt by al
    imageStyle:
      type: string
      enum:
        - none
        - ixen-dark
        - ixen-light
        - technical-diagram
        - cyberpunk-photo
        - educational
        - pencil-bw
        - pencil-color-accents
        - blueprint
      default: ixen-light
    imageBackground:
      type: string
      enum:
        - opaque
        - transparent
        - auto
      default: opaque
    imageModel:
      type: string
      default: gpt-image-1.5
    imageSize:
      type: string
      default: 1024x1024
    imageQuality:
      type: string
      enum:
        - auto
        - low
        - medium
        - high
      default: auto
    musicGenre:
      type: string
      default: ambient
    musicMood:
      type: string
      default: contemplative
    musicModel:
      type: string
      enum:
        - suno-ttapi
        - lyria-002
      default: suno-ttapi
    musicInstrumental:
      type: boolean
      default: false
    sunoVersion:
      type: string
      enum:
        - chirp-v3-0
        - chirp-v3-5
        - chirp-v4
        - chirp-v4-5
      default: chirp-v4-5
    ixens:
      type: array
      minItems: 1
      items:
        type: object
        properties:
          slug:
            type: string
          title:
            type: string
          description:
            type: string
          topic:
            type: string
          narrator:
            type: string
          details:
            type: string
          heroPrompt:
            type: string
          conceptPrompt1:
            type: string
          conceptPrompt2:
            type: string
        required:
          - slug
          - title
          - description
          - topic
          - narrator
          - details
          - heroPrompt
          - conceptPrompt1
          - conceptPrompt2
  required:
    - ixens
jobs:
  - name: images
    description: Generate the hero and supporting concept images for each Ixen.
    steps:
      - name: hero-${{ self.ixen.slug }}
        description: Generate the hero image for one Ixen topic.
        forEach:
          item: ixen
          in: ${{ inputs.ixens }}
        task:
          type: model_method
          modelType: "@alvagante/content-image"
          modelName: '${{ "image-" + self.ixen.slug + "-hero" }}'
          methodName: generate
          inputs:
            apiKey: ${{ vault.get(openai-keys, api-key) }}
            prompt: ${{ self.ixen.heroPrompt }}
            style: ${{ inputs.imageStyle }}
            model: ${{ inputs.imageModel }}
            background: ${{ inputs.imageBackground }}
            size: ${{ inputs.imageSize }}
            quality: ${{ inputs.imageQuality }}
            format: png
            filename: hero.png
            outputDir: ${{ "ixen/" + self.ixen.slug }}
        dependsOn: []
        weight: 0
        allowFailure: false
      - name: concept-1-${{ self.ixen.slug }}
        description: Generate the first supporting concept image for one Ixen topic.
        forEach:
          item: ixen
          in: ${{ inputs.ixens }}
        task:
          type: model_method
          modelType: "@alvagante/content-image"
          modelName: '${{ "image-" + self.ixen.slug + "-concept-1" }}'
          methodName: generate
          inputs:
            apiKey: ${{ vault.get(openai-keys, api-key) }}
            prompt: ${{ self.ixen.conceptPrompt1 }}
            style: ${{ inputs.imageStyle }}
            model: ${{ inputs.imageModel }}
            background: ${{ inputs.imageBackground }}
            size: ${{ inputs.imageSize }}
            quality: ${{ inputs.imageQuality }}
            format: png
            filename: concept-1.png
            outputDir: ${{ "ixen/" + self.ixen.slug }}
        dependsOn: []
        weight: 0
        allowFailure: false
      - name: concept-2-${{ self.ixen.slug }}
        description: Generate the second supporting concept image for one Ixen topic.
        forEach:
          item: ixen
          in: ${{ inputs.ixens }}
        task:
          type: model_method
          modelType: "@alvagante/content-image"
          modelName: '${{ "image-" + self.ixen.slug + "-concept-2" }}'
          methodName: generate
          inputs:
            apiKey: ${{ vault.get(openai-keys, api-key) }}
            prompt: ${{ self.ixen.conceptPrompt2 }}
            style: ${{ inputs.imageStyle }}
            model: ${{ inputs.imageModel }}
            background: ${{ inputs.imageBackground }}
            size: ${{ inputs.imageSize }}
            quality: ${{ inputs.imageQuality }}
            format: png
            filename: concept-2.png
            outputDir: ${{ "ixen/" + self.ixen.slug }}
        dependsOn: []
        weight: 0
        allowFailure: false
    dependsOn: []
    weight: 0
  - name: music
    description: Reuse existing soundtrack files when present, otherwise generate a fresh soundtrack.
    steps:
      - name: track-${{ self.ixen.slug }}
        description: Prepare the soundtrack playlist for one Ixen topic.
        forEach:
          item: ixen
          in: ${{ inputs.ixens }}
        task:
          type: model_method
          modelType: "@abnormalia/ixen-soundtrack"
          modelName: '${{ "soundtrack-" + self.ixen.slug }}'
          methodName: generateOrReuse
          inputs:
            apiKey: ${{ vault.get(onemin-keys, API_KEY) }}
            anthropicApiKey: ${{ vault.get(anthropic-keys, api-key) }}
            topic: ${{ self.ixen.topic }}
            title: ${{ self.ixen.title }}
            genre: ${{ inputs.musicGenre }}
            mood: ${{ inputs.musicMood }}
            instrumental: ${{ inputs.musicInstrumental }}
            model: ${{ inputs.musicModel }}
            sunoVersion: ${{ inputs.sunoVersion }}
            lyricsFallback: '${{ size(data.listVersions("music-" + self.ixen.slug, "song")) > 0 ? data.latest("music-" + self.ixen.slug, "song").attributes.lyrics : "" }}'
            outputDir: ${{ "ixen/" + self.ixen.slug }}
        dependsOn: []
        weight: 0
        allowFailure: false
    dependsOn: []
    weight: 0
  - name: page
    description: Generate the narrated Ixen page once hero art and music are ready.
    steps:
      - name: ixen-${{ self.ixen.slug }}
        description: Generate the minisite for one Ixen topic.
        forEach:
          item: ixen
          in: ${{ inputs.ixens }}
        task:
          type: model_method
          modelType: "@alvagante/content-ixen"
          modelName: '${{ "ixen-" + self.ixen.slug }}'
          methodName: generate
          inputs:
            apiFormat: ${{ inputs.ixenApiFormat }}
            apiKey: ${{ vault.get(anthropic-keys, api-key) }}
            baseUrl: ${{ inputs.ixenBaseUrl }}
            topic: ${{ self.ixen.topic }}
            narrator: ${{ self.ixen.narrator }}
            details: ${{ self.ixen.details }}
            mediaItems:
              - path: hero.png
                prompt: ${{ self.ixen.heroPrompt }}
              - path: concept-1.png
                prompt: ${{ self.ixen.conceptPrompt1 }}
              - path: concept-2.png
                prompt: ${{ self.ixen.conceptPrompt2 }}
            skillLevel: ${{ inputs.skillLevel }}
            outputLength: ${{ inputs.outputLength }}
            model: ${{ inputs.ixenModel }}
            credits: ${{ inputs.credits }}
            outputDir: ${{ "ixen/" + self.ixen.slug }}
            musicTracks: ${{ data.latest("soundtrack-" + self.ixen.slug, "playlist").attributes.tracks }}
        dependsOn: []
        weight: 0
        allowFailure: false
    dependsOn:
      - job: images
        condition:
          type: succeeded
      - job: music
        condition:
          type: succeeded
    weight: 0
version: 1
